# cargo-audit config (project root). Keep in sync with deny.toml advisory ignores.
[advisories]
ignore = [
    "RUSTSEC-2024-0436", # unmaintained paste via ratatui 0.28.1; upgrade blocked by MSRV 1.85
]
